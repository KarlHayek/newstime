{{#if timelines}}
<p>Click on a timeline to view its events</p>

{{else}}
<p>No timelines listed</p>
{{/if}}

{{#if searchResponse}}
<p>{{searchResponse}}</p>
{{/if}}


{{#each timelines}}
<div class="card card-body mb-2 timeline-card timeline-index">
    <a href="/timelines/show/{{id}}"></a>
    <h5>{{title}}</h5>
    <small><em><u>Topics:</u> {{topics}}</em></small>
    <p>{{details}}</p>
</div>
{{/each}}